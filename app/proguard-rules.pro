# Compose + Kotlin metadata are handled by the bundled consumer rules.
# Keep line numbers so personal crash reports stay readable.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
